{
  "name": "charts",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "yarn workspace @charts/web dev",
    "build": "yarn workspaces run build",
    "build:core": "yarn workspace @charts/core build",
    "test": "yarn workspaces run test"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "typescript": "^5.0.4"
  }
}